{
  "dependencies": {
    "token-transformer": "^0.0.29"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "style-dictionary": "^3.7.2"
  },
  "scripts": {
    "build": "npx token-transformer tokens.json tokens/light.json core,theme,light core && npx token-transformer tokens.json tokens/dark.json core,theme,dark core && npx style-dictionary build"
  }
}
